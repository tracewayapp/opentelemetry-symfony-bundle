receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

exporters:
  file:
    path: /output/traces.jsonl

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
